/*Övning 2.7: Gör ett programm som läser in bredd och längd på en rektangel och sedan
räknar ut omkretsen*/

#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include <float.h>
#include <math.h>
int main()
{
   system("chcp 1252");
   int bredd = () ;
   int langd = ();
   int omk = ();
   printf("Bestäm längden på rektangeln: ");
   scanf("%d", &langd);
   printf("Bestäm bredden på rektangeln: ");
   scanf("%d", &bredd);
   omk = (bredd * 2 + langd * 2);
   printf("Den totala omkretsen blir %d om längden är %d och bredden är %d", omk, langd, bredd);
   system("pause");
   return 0;

   
   
  
}
